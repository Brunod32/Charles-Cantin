---
layout: layout.njk
---
{% for home in collections.homes %}
    <h1 class="title-home text-center">{{home.data.title}}</h1>

    <p class="title-home text-center">{{home.data.subtitle}}</p>
    <a href="{{ '/galerie/index.html' }}">
        <img id="img-home" src={{ home.data.picture }} alt="Accueil"/>
    </a>
{% endfor %}
