---
layout: layout.njk
---
<h1 class="title-page">{{site.titleGallery}}</h1>

<main class="container-fluid">

    {# {% for gallerie in collections.galleries %} #}
        <ul>
            {# <li class="mx-3" href="#">{{ gallerie.data.title }}</li> #}
            <button type="button">Tout</button>
            <button type="button">Bébé</button>
            <button type="button">Mariage</button>
            <button type="button">Famille</button>
            <button type="button">Grossesse</button>
            <button type="button">Baptême</button>
            <button type="button">Couple</button>
            <button type="button">Portrait</button>
        </ul>
    {# {% endfor %} #}
     
    <hr>

    {# Bouclez et créez un balisage pour toutes les images dans /uplaods #}
    <section>
        {% for gallerie in collections.galleries %}
            <img src={{ gallerie.data.picture }} class="img-gallery" alt=""/>
        {% endfor %}
    </section>
    
</main>

